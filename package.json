{
  "name": "account-jammer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src --out-dir build",
    "start": "yarn build && node build/index.js"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "fast-csv": "^2.4.1",
    "inquirer": "^5.1.0",
    "request": "^2.85.0",
    "unistore": "^3.0.4"
  }
}
